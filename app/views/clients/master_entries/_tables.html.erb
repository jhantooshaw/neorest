<% unless @entries.blank? %>
  <div class="table-responsive" data-pattern="priority-columns">
    <table cellspacing="0" id="tech-companies-1" class="table table-small-font table-bordered table-striped">
      <thead>
        <tr>
          <th>Table Name</th>
          <th>Outlet</th>
          <th>Max Pax</th>
          <th data-priority="1">Position</th>
          <th>Section</th>
          <th data-priority="1">In Use</th>
          <th data-priority="1">Tax3 App</th>          

        </tr>
      </thead>
      <tbody>          
        <%@entries.each do |entry|%>
          <tr>
            <td><%= entry.t_name%></td>
            <td><%= entry.outlet.name if entry.outlet%></td>
            <td><%= entry.max_pax%></td>
            <td><%= entry.position%></td>
            <td><%= entry.table_section.section if entry.table_section%></td>
            <td><%= entry.inUse%></td>
            <td><%= entry.tax3app%></td>
          </tr>
        <%end%>
      </tbody>
    </table>
  </div>

  <div class="" style="text-align:center;">
    <%= will_paginate @entries, params: {location_id: params[:location_id], outlet_id: params[:outlet_id]}, class: 'pagination-sm', renderer: BootstrapPagination::Rails %>
    </div>
  <%else%>    
    No Table Found!!!
  <%end%>
  <script type="text/javascript">
    responsive_table(true);
  </script>